<nav class="navbar navbar-default">
  <div class="container">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
    </div>
    <i class="fa fa-twitter fa-2x navbar-brand" style="transform: rotateX(180deg);"></i>
    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <% if user_signed_in? %>
          <li><a href="/home"><i class="fa fa-home"></i> Home <span class="sr-only">(current)</span></a></li>
          <li><a href="/home"><i class="fa fa-bell"></i> Notifications <span class="sr-only">(current)</span></a></li>
          <li><a href="/home"><i class="fa fa-paper-plane"></i> Messages <span class="sr-only">(current)</span></a></li>
        <% else %>
          <li><a href="/home">Home <span class="sr-only">(current)</span></a></li>
          <li><a href="#">Explore</a></li>
        <% end %>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <li>
          <form class="navbar-form navbar-right" role="search">
            <div class="form-group">
              <input type="text" class="form-control" placeholder="Search Pakutter">
            </div>
          </form>
        </li>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false" width="25" height="25">
            <% if user_signed_in? %>
              <img src="/baracktocat.jpg" width="25" height="25">
              <!-- <%= current_user.username %> -->
            <% else %>
              Navigation
            <% end %> <span class="caret"></span></a>
          <ul class="dropdown-menu">
            <% if user_signed_in? %>
              <li><a href="/user/<%= current_user.username %>">Profile</a></li>
              <li role="separator" class="divider"></li>
              <li><a href="#">Help</a></li>
              <li role="separator" class="divider"></li>
              <li><%= link_to "Settings", edit_user_registration_path %></li>
              <li role="separator" class="divider"></li>
              <li><%= link_to "Log Out", destroy_user_session_path, method: :delete %></li>
            <% else %>
              <li><a href="#">Help</a></li>
              <li role="separator" class="divider"></li>
              <li><%= link_to "Log In", new_user_session_path %></li>
              <li role="separator" class="divider"></li>
              <li><%= link_to "Sign Up", new_user_registration_path %></li>
            <% end %>
          </ul>
        </li>
        <% if user_signed_in? %>
          <li class="btn btn-primary navbar-btn">Add Post</li>
        <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
